{
    "admin": {
        // Dirección IP y puerto en el que se escuchan las peticiones administrativas
        "listen": "167.235.155.72:2019"
    },
    "apps": {
        "http": {
            "servers": {
                "Cloud_Guardian": {
                    // Configuración del servidor llamado "Cloud_Guardian"
                    "listen": [":80"],  // El servidor escucha en el puerto 80 (HTTP estándar)
                    "timeouts": {
                        // Establece los tiempos de espera para varias acciones del servidor
                        "read_header_timeout": "15s", // Tiempo máximo para leer los encabezados (15 segundos)
                        "idle_timeout": "5m", // Tiempo máximo sin actividad antes de cerrar la conexión (5 minutos)
                        "write_timeout": "15s", // Tiempo máximo para escribir la respuesta (15 segundos)
                        "header_timeout": "10s" // Tiempo máximo para leer los encabezados (10 segundos)
                    },
                    "max_connections": 50,  // Número máximo de conexiones simultáneas permitidas
                    "max_requests_per_connection": 20,  // Número máximo de solicitudes por conexión
                    "routes": [
                        {
                            // Ruta que se aplica cuando el camino de la URL es "/login"
                            "match": [
                                {
                                    "path": ["/login"]
                                }
                            ],
                            "handle": [
                                {
                                    // Limita el número de solicitudes por minuto a 3 para la ruta "/login"
                                    "handler": "rate_limit",
                                    "rate_limit": {
                                        "requests": 3,  // Permite 3 solicitudes
                                        "window": "1m"  // Dentro de un intervalo de 1 minuto
                                    }
                                },
                                {
                                    // Requiere autenticación básica para acceder a la ruta "/login"
                                    "handler": "authenticate",
                                    "basic": {
                                        "users": {
                                            "admin": "1234",  // Usuario admin con la contraseña 1234
                                            "Alberto": "5678",  // Usuario Alberto con la contraseña 5678
                                            "Ian": "9123",  // Usuario Ian con la contraseña 9123
                                            "Jose": "4567",  // Usuario Jose con la contraseña 4567
                                            "Jaime": "8901"  // Usuario Jaime con la contraseña 8901
                                        }
                                    }
                                },
                                {
                                    // Respuesta estática que se muestra tras una autenticación exitosa
                                    "handler": "static_response",
                                    "body": "Acceso a /login concedido"  // Mensaje mostrado al usuario después de autenticarse correctamente
                                }
                            ]
                        },
                        {
                            // Ruta que se aplica cuando el camino de la URL es "/"
                            "match": [
                                {
                                    "path": ["/"]
                                }
                            ],
                            "handle": [
                                {
                                    // Respuesta estática para la página principal
                                    "handler": "static_response",
                                    "body": "Página principal accesible para IPs permitidas"  // Mensaje mostrado en la página principal
                                }
                            ]
                        }
                    ]
                }
            },
            "security": {
                "remote_ip": {
                    // Configuración de seguridad para permitir o denegar el acceso según la IP
                    "allow": ["100.10.10.1", "192.168.1.100"],  // Permitir acceso a las IPs específicas
                    "deny": ["0.0.0.0/0", "192.168.0.0/24"]  // Denegar acceso a todas las demás IPs, incluyendo el rango 192.168.0.0/24
                }
            }
        }
    }
}